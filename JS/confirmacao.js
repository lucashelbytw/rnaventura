// var carrinho = JSON.parse(localStorage.getItem("carrinho"));
// var total = parseFloat(localStorage.getItem("total"));

// var listaItens = document.getElementById("itensComprados");
// var totalCompra = document.getElementById("totalCompras"); // Corrija o ID aqui

// if (carrinho && carrinho.length > 0) {
//     for (var i = 0; i < carrinho.length; i++) {
//         var item = document.createElement("li");
//         item.textContent = carrinho[i].nome + " R$ " + carrinho[i].preco;
//         listaItens.appendChild(item);
//     }
// }

// if (!isNaN(total)) {
//     totalCompra.textContent = "Total da Compra: R$ " + total.toFixed(2);
// } else {
//     totalCompra.textContent = "Total da Compra: R$ 0.00";
// }
